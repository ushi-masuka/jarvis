# Priority Fixes & Core Improvements

## 1. Critical Fetcher Bugs
- [ ] **ArXiv Fetcher:** Investigate and fix poor quality results.
- [ ] **Medium Fetcher:** Resolve outstanding issues causing errors.
- [ ] **Blog Fetcher:** Correct the usage of `trafilatura` to remove warnings and improve stability.

## 2. Data Extraction & Ingestion
- [ ] **Multi-Layered Extraction:** Implement the tiered extractor chain (`trafilatura`, `readability-lxml`, etc.) in `utils/extraction.py`.
- [ ] **PDF Extraction:** Add a new function for robust PDF text extraction using `PyMuPDF`.
- [ ] **Data Sanitization:** Add a step in `ingest.py` to sanitize metadata before upserting to the vector DB to prevent errors.
- [ ] **Reporting:** Add a summary report at the end of `data_fetcher.py` to track fetch/extraction success rates.

## 3. Query Processing
- [ ] **Query Revamp:** Improve `query_processor.py` to better handle special characters/words.
- [ ] **LLM-based Generation:** Explore using an LLM to generate source-specific queries for fetchers.

## 4. Future Enhancements
- [ ] **New Fetchers:** Add fetchers for web forums, social media, etc.
- [ ] **Paywall Detection:** Implement a feature to identify and report content that is behind a paywall.